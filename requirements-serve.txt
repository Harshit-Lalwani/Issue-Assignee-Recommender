# Dependencies for the deployed serving container (Dockerfile), generated from pyproject.toml's
# [project.dependencies] minus torch -- torch is installed separately as the CPU-only wheel
# (see Dockerfile) so it doesn't drag in ~2GB of CUDA libraries no free CPU tier can use.
duckdb>=1.0
pandas>=2.2
numpy>=1.26
scipy>=1.11
pyarrow>=16.0
orjson>=3.10
tqdm>=4.66
rank-bm25>=0.2.2
sentence-transformers>=3.0
faiss-cpu>=1.8
lightgbm>=4.3
scikit-learn>=1.4
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
requests>=2.31
huggingface_hub>=0.24
